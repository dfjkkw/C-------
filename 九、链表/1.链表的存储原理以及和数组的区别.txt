//链表的存储原理以及数组的区别
/*
链表是一种线性数据结构，同一类型的不同实例可以保存在一个链表当中，位于第一个位置的元素可做表头，该元素的结构内部会拥有一个指针，
指向第二个元素所在的地址，第二个元素也会有一个指针指向第三个元素所在的地址······知道最末一个元素。最末一个元素也会拥有一个指向
下一个元素的指针，但它已没有下一元素，于是一般来讲我们让它这个指针指向NULL.
之前学习的一维数组也是线性数据结构。但是，一维数组的各个元素无论在概念上还是在物理地址上都是处于一种线性排列关系，而链表中的各
个元素仅仅是在概念上处于线性排列的关系
*/